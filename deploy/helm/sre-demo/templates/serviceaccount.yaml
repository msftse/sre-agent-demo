apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "sre-demo.serviceAccountName" . }}
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "sre-demo.labels" . | nindent 4 }}
automountServiceAccountToken: false
